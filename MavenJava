pipeline {
    agent none 
    stages {
        stage('Example Build') {
            agent { docker 'gradle:latest' } 
            steps {
                echo 'Hello, gradle'
                sh 'gradle clean'
                sh 'gradle build'
            }
        }
    }
}
