pipeline {
    agent none 
    stages {
        stage('Example Build') {
            agent { docker 'gradle:4.7.0-jdk8-alpine' } 
            steps {
                echo 'Hello, gradle'
                sh 'gradle --version'
            }
        }
        stage('Example Test') {
            agent { docker 'openjdk:8-jre' } 
            steps {
                echo 'Hello, JDK'
                sh 'java -version'
            }
        }
    }
}
